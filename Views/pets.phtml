<?php require('template/header.phtml'); ?>

<div class="container mt-4">
    <h3 class="mb-4">All Registered Pets</h3>

    <!-- Feedback Messages -->
    <?php if (!empty($view->successMessage)): ?>
        <div class="alert alert-success"><?= htmlspecialchars($view->successMessage) ?></div>
    <?php endif; ?>

    <?php if (!empty($view->errorMessage)): ?>
        <div class="alert alert-danger"><?= htmlspecialchars($view->errorMessage) ?></div>
    <?php endif; ?>

    <?php if (empty($view->pets)): ?>
        <div class="alert alert-info">No pets have been registered yet.</div>
    <?php else: ?>
        <div class="row">
            <?php foreach ($view->pets as $pet): ?>
                <?php
                $status = strtolower($pet->getStatus());
                $statusClass = ($status === 'lost') ? 'border-danger' : 'border-success';
                $statusText = ucfirst($status);
                ?>

                <div class="col-md-4 mb-4">
                    <div class="card h-100 <?= $statusClass ?> border-start border-4 shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title">
                                <?= htmlspecialchars($pet->getName()) ?>
                                (<?= htmlspecialchars($pet->getSpecies()) ?>)
                            </h5>

                            <p class="card-text mb-1">
                                <strong>Status:</strong>
                                <span class="fw-bold text-<?= ($status === 'lost') ? 'danger' : 'success' ?>">
                                    <?= htmlspecialchars($statusText) ?>
                                </span><br>
                                <strong>Breed:</strong> <?= htmlspecialchars($pet->getBreed() ?: 'Unknown') ?><br>
                                <strong>Color:</strong> <?= htmlspecialchars($pet->getColor() ?: 'Unknown') ?><br>
                                <strong>Description:</strong> <?= htmlspecialchars($pet->getDescription() ?: 'Not provided') ?><br>
                                <strong>Date Reported:</strong> <?= htmlspecialchars($pet->getFormattedReportDate() ?? 'Unknown') ?>
                            </p>

                            <?php if (isset($_SESSION['role']) && $_SESSION['role'] === 'admin'): ?>
                                <!-- Buttons: Edit & Delete -->
                                <div class="d-flex justify-content-end gap-2 mt-3">
                                    <button class="btn btn-sm btn-outline-primary" type="button"
                                            data-bs-toggle="collapse"
                                            data-bs-target="#editForm<?= $pet->getId() ?>"
                                            aria-expanded="false" aria-controls="editForm<?= $pet->getId() ?>">
                                        Edit
                                    </button>

                                    <form method="post" action="pets.php" onsubmit="return confirm('Delete this pet?');">
                                        <input type="hidden" name="delete_pet" value="<?= htmlspecialchars($pet->getId()) ?>">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                    </form>
                                </div>

                                <!-- Inline Edit Form -->
                                <div class="collapse mt-3" id="editForm<?= $pet->getId() ?>">
                                    <form method="post" action="pets.php" class="border-top pt-3">
                                        <input type="hidden" name="pet_id" value="<?= htmlspecialchars($pet->getId()) ?>">

                                        <div class="mb-2">
                                            <label class="form-label small mb-0">Name</label>
                                            <input type="text" name="name" class="form-control form-control-sm"
                                                   value="<?= htmlspecialchars($pet->getName()) ?>">
                                        </div>

                                        <div class="mb-2">
                                            <label class="form-label small mb-0">Species</label>
                                            <input type="text" name="species" class="form-control form-control-sm"
                                                   value="<?= htmlspecialchars($pet->getSpecies()) ?>">
                                        </div>

                                        <div class="mb-2">
                                            <label class="form-label small mb-0">Breed</label>
                                            <input type="text" name="breed" class="form-control form-control-sm"
                                                   value="<?= htmlspecialchars($pet->getBreed()) ?>">
                                        </div>

                                        <div class="mb-2">
                                            <label class="form-label small mb-0">Color</label>
                                            <input type="text" name="color" class="form-control form-control-sm"
                                                   value="<?= htmlspecialchars($pet->getColor()) ?>">
                                        </div>

                                        <div class="mb-2">
                                            <label class="form-label small mb-0">Photo URL</label>
                                            <input type="text" name="photo_url" class="form-control form-control-sm"
                                                   value="<?= htmlspecialchars($pet->getPhotoUrl()) ?>">
                                        </div>

                                        <div class="mb-2">
                                            <label class="form-label small mb-0">Status</label>
                                            <select name="status" class="form-select form-select-sm">
                                                <option value="lost" <?= $pet->getStatus() === 'lost' ? 'selected' : '' ?>>Lost</option>
                                                <option value="found" <?= $pet->getStatus() === 'found' ? 'selected' : '' ?>>Found</option>
                                            </select>
                                        </div>

                                        <div class="mb-2">
                                            <label class="form-label small mb-0">Description</label>
                                            <textarea name="description" class="form-control form-control-sm" rows="2"><?= htmlspecialchars($pet->getDescription()) ?></textarea>
                                        </div>

                                        <div class="d-flex justify-content-end">
                                            <button type="submit" name="update_pet" class="btn btn-sm btn-success">Save Changes</button>
                                        </div>
                                    </form>
                                </div>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>
</div>

<?php require('template/footer.phtml'); ?>
