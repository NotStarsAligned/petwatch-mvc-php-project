<?php require('template/header.phtml') ?>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <h1 class="mb-4 text-center">Report a Pet Sighting</h1>
            <hr>

            <?php if ($view->errorMessage): ?>
                <div class="alert alert-danger"><?= htmlspecialchars($view->errorMessage) ?></div>
            <?php endif; ?>

            <?php if ($view->successMessage): ?>
                <div class="alert alert-success"><?= htmlspecialchars($view->successMessage) ?></div>
                <div class="text-center mt-3">
                    <a href="index.php" class="btn btn-outline-primary">Return to Sightings</a>
                </div>
            <?php else: ?>

                <form method="post" action="report.php" class="card p-4 shadow-sm">
                    <div class="mb-3">
                        <label for="pet_id" class="form-label">Select Pet:</label>
                        <select name="pet_id" id="pet_id" class="form-select" required>
                            <option value="">-- Choose a Pet --</option>
                            <?php foreach ($allPets as $pet): ?>
                                <option value="<?= htmlspecialchars($pet->getId()) ?>">
                                    <?= htmlspecialchars($pet->getName()) ?> (<?= htmlspecialchars($pet->getSpecies()) ?>)
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="comment" class="form-label">Comment / Description:</label>
                        <textarea name="comment" id="comment" rows="3" class="form-control" required></textarea>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="latitude" class="form-label">Latitude:</label>
                            <input type="text" name="latitude" id="latitude" class="form-control" required>
                        </div>
                        <div class="col-md-6">
                            <label for="longitude" class="form-label">Longitude:</label>
                            <input type="text" name="longitude" id="longitude" class="form-control" required>
                        </div>
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <a href="index.php" class="btn btn-secondary">Cancel</a>
                        <button type="submit" name="report_submit" class="btn btn-success">Submit Report</button>
                    </div>
                </form>

            <?php endif; ?>
        </div>
    </div>
</div>

<?php require('template/footer.phtml') ?>
