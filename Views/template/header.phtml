<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>petWatch - <?= htmlspecialchars($view->pageTitle ?? 'Home') ?></title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<?php
echo "<!-- Debug: Role is " . htmlspecialchars($_SESSION['role'] ?? 'not set') . " -->";
?>

<body class="d-flex flex-column h-100">
<header>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <a class="navbar-brand" href="index.php">petWatch</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse"
                    aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <?php
            $currentPage = basename($_SERVER['PHP_SELF']);
            $isLoggedIn = !empty($_SESSION['user_id']);
            $userRole = $_SESSION['role'] ?? null;
            ?>

            <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                <!-- LEFT SIDE  -->
                <ul class="navbar-nav mb-2 mb-md-0">
                    <li class="nav-item">
                        <a class="nav-link <?= $currentPage === 'index.php' ? 'active' : '' ?>" href="index.php">Home</a>
                    </li>

                    <?php if ($isLoggedIn): ?>
                        <li class="nav-item">
                            <a class="nav-link <?= $currentPage === 'report.php' ? 'active' : '' ?>" href="report.php">Report Sighting</a>
                        </li>

                        <?php if ($userRole === 'admin'): ?>
                            <li class="nav-item">
                                <a class="nav-link <?= $currentPage === 'add_pet.php' ? 'active' : '' ?>" href="add_pet.php">Add Pet</a>
                            </li>

                            <li class="nav-item">
                                <a class="nav-link <?= $currentPage === 'pets.php' ? 'active' : '' ?>" href="pets.php">View Pets</a>
                            </li>
                        <?php endif; ?>

                    <?php else: ?>
                        <li class="nav-item">
                            <a class="nav-link <?= $currentPage === 'login.php' ? 'active' : '' ?>" href="login.php">Login</a>
                        </li>
                    <?php endif; ?>

                </ul>

                <!-- RIGHT SIDE -->
                <?php if ($isLoggedIn): ?>
                    <div class="d-flex align-items-center ms-auto">
                        <span class="navbar-text text-white me-3">
                            Welcome, <?= htmlspecialchars($view->username ?? 'User') ?>
                        </span>
                        <form method="post" action="login.php" class="d-inline m-0">
                            <input type="hidden" name="logout" value="1">
                            <button class="btn btn-outline-light btn-sm" type="submit">Logout</button>
                        </form>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </nav>
</header>

<!-- This just exists to add some space between the Nav bar and the page itself. -->
<main class="flex-shrink-0 mt-5 pt-4">
